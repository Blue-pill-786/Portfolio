export default function Footer() {
  return (
    <footer className="py-12 text-center text-sm text-gray-500">
      <p>
        © {new Date().getFullYear()} Ubair — Built with React & Tailwind
      </p>
    </footer>
  );
}
